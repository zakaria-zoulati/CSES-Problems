    #include <iostream>
    #include <vector>
    #include <algorithm>

    using namespace std ; 
    using ll = long long ; 

    int main(){
        int n ; cin >> n ; 
        vector< pair<int,int> > times( n ) ; 
        for( int i=0 ; i<n ; ++i ){
            int a , b ; 
            cin >> a >> b ; 
            times[i] = {b , a};
        }
        sort( times.begin() , times.end() ) ; 
        int rs = 0 ;
        int start = n-1 ; 
        int curr = 1 ; 
        int s = times[]
        for( int i=n-1 ; i>=0 ; --i ){
            int curr 
        
        }   
        cout << rs << endl ; 
        return 0 ; 
    }